var input = document.getElementById('input-number');

// Append the option elements
for ( var i = 0; i <= 1256; i++ ){

	var input-number = document.createElement("number");
		input-number.text = i;
		input-number.value = i;

	input.appendChild(input-number);
}